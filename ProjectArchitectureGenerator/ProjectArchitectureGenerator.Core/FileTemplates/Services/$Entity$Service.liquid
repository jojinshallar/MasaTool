{{- if model.includelocalusing -}}
using {{ model.projectnamespace }}.Application.{{ model.entityname }}.Commands;
{{ raw }}
{{ endraw }}
{{- end -}}
namespace {{ model.projectnamespace }}.Services
{
    public class {{ model.entityname }}Service : ServiceBase
    {
        public async Task<IResullt> CreateAsync(Create{{ model.entityname }}Command command)
        {
            throw new NotImplementedException();
        }

        public async Task<IResult> DeleteAsync(Delete{{ model.entityname }}Command command)
        {
            throw new NotImplementedException();
        }
    }
}